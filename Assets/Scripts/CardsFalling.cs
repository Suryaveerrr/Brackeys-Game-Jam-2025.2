using UnityEngine;

public class CardsFalling : MonoBehaviour
{
  

    // Update is called once per frame
    void Update()
    {
        //rotate the object around its local X axis at 1 degree per second
       // transform.Rotate(Vector3.right * Time.deltaTime * 500);
    }
}
